# Claude Code local files (skills, plans, etc.) - not part of the product
.claude/

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

# Dependencies (anywhere in the tree, e.g. vendored skill scripts)
node_modules/

# Env files (anywhere in the tree)
.env
.env.*
!.env.example

# Build/cache artifacts (anywhere in the tree)
.next/
*.tsbuildinfo

# Local tooling and temporary source snapshots
supabase/.temp/
web/all_dash_files.txt
web/has_language_files.txt
*.backup.tsx

# Agent skill packages (e.g. `npx skills add`) - dev tooling, not the product
/.agents/
/skills-lock.json

# Video-feature scratch: standalone prototype project, test render outputs,
# and one-off test scripts/audio - not part of the shipped feature
/promo-video/
/rendered-test-videos/
web/test-render.ts
web/render-viral-benchmark.ts
web/public/audio/test-voice.mp3
web/public/audio/test-voice.vtt
web/public/audio/voiceover-*.mp3
web/scratch/
